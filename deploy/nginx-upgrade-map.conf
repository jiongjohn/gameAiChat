# WebSocket / SSE Connection-upgrade map for AI Companion Chat.
# Install to /etc/nginx/conf.d/upgrade-map.conf
# Referenced by deploy/nginx.conf via $connection_upgrade.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      '';
}
